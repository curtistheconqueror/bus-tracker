# Publish next

**STATUS: NONE PENDING**

Everything currently on `main` is live through Sites Version 114, source commit
`cc662ac`.

Claude Code replaces this status with `STATUS: PENDING` in the same push that
adds the next validated release. That handoff must name the source boundary,
what changed, any migration, and what Codex should check once live.

Codex stops here when the status is `NONE PENDING`. Publishing still requires
Curtis's explicit approval, including the shorthand **publishing approved**.

Follow `docs/SITES_PUBLISHING_RUNBOOK.md` for the granular Sites lifecycle.
