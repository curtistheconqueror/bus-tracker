## What changed

<!-- In the words Curtis would use: what was wrong on the floor, and what the
     app does now. Function names are for the diff, not for this box. -->

## Why

<!-- The reason, including what went wrong before if this is a fix. -->

## Gates run

<!-- All three, with what they actually said. Re-run them; do not quote a
     number from earlier in the session. -->

- [ ] `npm test` (builds first, then every file in `tests/`) — result:
- [ ] `npm run lint` — result:
- [ ] `npm run build` — result:

UI or flow change? If this moves a control, renames a label, reorders a list or
changes what a tap does, say so here and get Curtis's word before it merges.

## Codex publish note

<!-- Codex publishes; Claude Code does not. Say whether this needs a new
     version section in docs/PUBLISH_NEXT.md when it merges, and name any
     path in PROJECT_HANDOFF.md, docs/RELEASES.md or
     docs/SITES_PUBLISHING_RUNBOOK.md that Codex now has to update. -->

- New `docs/PUBLISH_NEXT.md` version section needed:
- New LocalStorage keys (adding is fine, renaming never is):
- Supabase migration required:
